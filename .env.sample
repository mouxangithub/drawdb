VITE_BACKEND_URL=http://backend.com
VITE_GITHUB_ACCESS_TOKEN=my_access_toke

# API配置
# 如果不设置，启动脚本会根据PORT环境变量自动生成
# 默认端口(80/443)使用相对路径/api，非默认端口使用完整URL
API_URL=http://localhost:3001/api

# 服务器配置
PORT=3001
NODE_ENV=development

# 数据库配置
DB_STORAGE=server/database/drawdb.sqlite

# JWT 配置（如需实现用户认证）
JWT_SECRET=mouxan
JWT_EXPIRES_IN=7d 